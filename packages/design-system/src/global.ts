// VE global styles — coexists with existing CSS cascade
// Import design-system theme to inject :root VE vars
import "./theme.css.ts";

// Import per-element reset
import "./reset.css.ts";

// Import global defaults
import "./defaults.css.js";
