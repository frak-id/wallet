<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>FrakMerchantApp</string>
    <key>CFBundleDisplayName</key>
    <string>Frak Merchant</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <!--
      Landscape is here so the sharing sheet's safe-area handling can be exercised.
      Portrait-only made the notch-on-the-side case untestable in the one app that
      drives the SDK.
    -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <!--
      Both wallet variants: the app ships as `frakwallet://` in prod and
      `frakwallet-dev://` for the dev stage (see sdk/core/src/constants.ts).
      Declaring only the prod scheme makes a locally-built dev wallet
      undetectable via canOpenURL — exactly the build a POC runs against.
    -->
    <key>LSApplicationQueriesSchemes</key>
    <array>
        <string>frakwallet</string>
        <string>frakwallet-dev</string>
    </array>
    <!-- Inbound referral links: `merchantapp://product?fCtx=…`. -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>id.frak.example.ios</string>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>merchantapp</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
