---
"@frak-labs/core-sdk": minor
"@frak-labs/react-sdk": minor
---

✨ feat: Add `preload` option to `FrakWalletSdkConfig` allowing consumers to specify which views inside the listener iframe should be preloaded. Defaults to `["sharing"]`.
