import { style } from "@vanilla-extract/css";

export const setupRecoveryDisclaimer = style({
    marginBottom: 20,
});

export const setupRecoveryDisclaimerIcon = style({
    margin: "0 5px 5px 0",
    verticalAlign: "middle",
});
