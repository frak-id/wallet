import { style } from "@vanilla-extract/css";

export const iconWrapper = style({
    flexShrink: 0,
    paddingTop: "2px",
});

export const content = style({
    minWidth: 0,
    flex: "1 0 0",
});
