<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.app.Splash"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <!-- Asset statements for WebAuthn Digital Asset Links verification -->
        <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Deep-link intent-filters (HTTPS App Links + custom schemes) are injected
                automatically by `tauri-plugin-deep-link`'s build.rs from `tauri.conf.json >
                plugins.deep-link.mobile`. Do NOT re-declare them here — the plugin needs the
                runtime config (so its Kotlin `isDeepLink()` filter accepts the URL) and the
                manifest declaration (so Android delivers the intent), and adding both would
                produce duplicate intent-filters in the final manifest.

                Per-variant routing is enforced by App Link verification: the `assetlinks.json`
                served by each backend (wallet.frak.id vs wallet-dev.frak.id) only delegates
                to the matching package id (id.frak.wallet vs id.frak.wallet.dev), so even
                though every variant declares both hosts in its manifest, only the matching
                host actually opens the app.
            -->
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
