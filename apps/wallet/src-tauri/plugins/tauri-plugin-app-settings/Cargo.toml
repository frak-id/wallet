[package]
name = "tauri-plugin-app-settings"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-app-settings"

[dependencies]
tauri = "2"
serde = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
