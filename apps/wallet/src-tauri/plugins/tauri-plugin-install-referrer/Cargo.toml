[package]
name = "tauri-plugin-install-referrer"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-install-referrer"

[dependencies]
tauri = "2"
serde = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
