{
    "$schema": "../gen/schemas/mobile-schema.json",
    "identifier": "android",
    "description": "Android-specific permissions including WebAuthn, Share, and FS",
    "platforms": ["android"],
    "windows": ["main"],
    "permissions": [
        "webauthn:default",
        "share:default",
        "fs:default",
        "fs:allow-download-write"
    ]
}
