/**
 * Custom CSS editing (per-component, global, and per-placement) is hidden for
 * now and will be brought back in a later iteration. Stored `rawCss` values are
 * preserved untouched while the inputs are gated off.
 */
export const CUSTOM_CSS_ENABLED = false;
