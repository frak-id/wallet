import { style } from "@vanilla-extract/css";

export const selectCurrency = style({
    display: "flex",
    flexDirection: "column",
    marginBottom: "20px",
});

export const selectCurrencyLabel = style({
    marginBottom: "10px",
});
