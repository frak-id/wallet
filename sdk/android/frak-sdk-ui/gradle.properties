# Per-module POM identity. The rest of the POM (licence, developers, SCM, url)
# is shared - see buildSrc/src/main/kotlin/frak-publish.gradle.kts.
frak.pom.name=Frak SDK UI
frak.pom.description=Frak native Android SDK, sharing sheet UI. Depends on id.frak.sdk:core; kept separate so a merchant taking only tracking never pulls in a web view.
